<Window x:Class="TimeStudiesWpf.Dialogs.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="About REFA Time Study"
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock Text="⏱️" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock Text="REFA Time Study" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
            <TextBlock Text="Zeitaufnahme-Anwendung" FontSize="14" Foreground="Gray"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Info -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Version 1.0.0" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text=".NET 10.0 WPF Application" HorizontalAlignment="Center"
                       Foreground="Gray" Margin="0,0,0,16"/>
            <TextBlock TextAlignment="Center" Foreground="Gray">
                <Run Text="Industrial time study application following"/>
                <LineBreak/>
                <Run Text="REFA methodology standards."/>
            </TextBlock>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="2" Content="OK" Width="80" Padding="8,4"
                HorizontalAlignment="Center" Click="OkButton_Click" IsDefault="True"/>
    </Grid>
</Window>
